class SquadModel{
  String? id;
  String? name;
  String? photo;
  String? age;
  String? number;
  String? position;

  SquadModel(this.id, this.name, this.photo, this.age, this.number, this.position);
}